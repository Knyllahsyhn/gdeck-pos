{
  "name": "gdeck-pos",
  "version": "1.5.0",
  "private": true,
  "description": "Hüttenkasse: offline point of sale in a single HTML file",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs && wrangler dev",
    "deploy": "node build.mjs && wrangler deploy",
    "test": "node build.mjs && node test/run.mjs"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "jsdom": "^28.1.0",
    "wrangler": "^4.125.0"
  }
}
